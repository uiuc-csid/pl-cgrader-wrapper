# Question grading tests

Place the contents of your PL question's `tests` directory here. The wrapper entrypoint will try to find either `test.py` or `tests.py` at runtime to minimize configuration problems.

For more information, please see the general notes in the wrapper's overview `README.md` file.
